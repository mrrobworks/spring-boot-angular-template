import { TestBed } from '@angular/core/testing';
import { ShowForPermissionDirective } from './show-for-permission.directive';

describe('ShowForPermissionDirective', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ShowForPermissionDirective]
    });
  });
});
