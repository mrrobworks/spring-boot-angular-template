import { TestBed } from '@angular/core/testing';
import { EnableForPermissionDirective } from './enable-for-permission.directive';

describe('EnableForPermissionDirective', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [EnableForPermissionDirective]
    });
  });
});
